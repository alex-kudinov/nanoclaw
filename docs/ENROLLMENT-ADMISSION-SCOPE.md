# Local authenticated admission scope

NC-20260906-009. Owner-authorized local foundation only, based on `a382e215`.
The previous mixed completion condition is split: local authenticated admission
can complete from synthetic/disposable proof, while
`work:student-enrollment-production-admission-pilot` retains the original live
promotion/new-event condition and remains unauthorized. Authority cutover also
depends on that pilot. No consequential commitment was discarded.

Implement registered host issuer authentication for normalized provider-event,
provider-read and named operator evidence; exact purpose/role/source binding;
idempotent store invocation; shared legacy/new writer exclusion. All keys,
provider messages and operator identities are synthetic/mocked. A local signed
issuer protocol is not a claim of native Stripe/Plutio API compatibility.
Real native verification adapters, credentials and source retrieval are pilot
requirements, not implemented or exercised by this foundation.

Production store promotion, migrations, data/locking policy, legacy writer
participation, target/population and rollback require a separate decision.
No real credentials/provider reads or writes, history, production migration,
deployment, writer cutover, payment/refund or communication is authorized.
