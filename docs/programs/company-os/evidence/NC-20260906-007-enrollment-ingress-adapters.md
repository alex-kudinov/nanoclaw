# NC-20260906-007 enrollment ingress evidence

Work item: `work:student-enrollment-multisource-ingress-adapters`.
Authority: `decision-student-enrollment-ingress-adapters-2026-09-06` records the
owner's proceed instruction for the local synthetic adapter slice only.

## Orientation and source

- Company OS validated at r226 with no active claim. Accepted authorization
  moved the existing candidate to ready at r227; claim r228; nine explicit
  continuity commitments attached at r229. Validation clean; sole claim NC-007.
- Dirty primary remains `codex/continuity-reconciliation` at `51185a5d`.
  Isolated source worktree `/Users/xbohdpukc/dev/NanoClaw-enrollment-ingress-20260906`
  starts from exact reviewed/pushed predecessor `2755a284` on
  `codex/student-enrollment-ingress-adapters-20260906`.
- Registration `ea6ec317` was committed/pushed before source changes. No
  concurrent program claim or source overlap; live payment/capacity writers
  and the reviewed predecessor engines remain unchanged.

## Implementation

See `docs/STUDENT-ENROLLMENT-INGRESS-ADAPTERS.md`. The new pure adapter consumes
synthetic normalized candidate snapshots plus separate trusted host proofs,
not raw provider bodies. It verifies exact proof binding and roles, normalizes
funding aliases, attaches minimized evidence, and reuses canonical commands.
Every admitted source route creates canonical state or an owned exception.
Migration/correction intake is an owned resolution gate, not a replay executor.

Accepted local source paths and exclusions are explicit: native authentication
and retrieval, transactional persistence, real-data/provider actions, runtime
wiring, historical replay, deployment, financial actions and communication are
not implemented or exercised by this slice. The proof registry is a trusted
host boundary; a digest of an untrusted proposal cannot authenticate itself.

## Verification and review

- Isolated dependencies installed with pinned Node 22.23.2 (`npm ci`).
- Focused 165/165 (10 files), including 34 adapter cases and all selected
  Bookkeeper/enrollment/capacity/fulfillment regressions. Typecheck passes.
- The two full-suite failures reproduce on unchanged predecessor `2755a284`
  (8 pass / 2 fail): CNPC wrapper literal and date-sensitive Trafft freshness.
- Sonnet/high R1 session `840f4936-6057-4e45-b899-48a165b52fd2`, fresh bounded
  Read/Write-only review of exactly three files, 100k compaction threshold,
  via the installed rotation runner (`alex,info`; no cnpc). Verdict pending.
- Final full-suite/continuity results, review dispositions, usage, Git and
  program closure receipts will be appended; no live success is claimed.

## Final verification and independent review

- Final focused: **166/166**, ten files; 35 new adapter cases. Pinned Node
  22.23.2 typecheck passes. Full root: **3,699 pass / 32 skipped / 2 fail**;
  345 test files pass, ten skipped, two fail. Both failures remain the exact
  independently reproduced predecessor CNPC/Trafft failures above.
- Documentation continuity passes: 164 task rows / 160 changelog entries;
  schema sanitizer and generated capability matrix check pass.
- R1 reported that missing participant proof wrongly materialized a capacity
  commitment. Codex executed the precise proposed case: zero enrollments,
  entitlements, class assignments and projections; one unassigned seat, owned
  participant_missing exception and held funded commitment. This is required
  by the earlier accepted Bookkeeper/capacity contract. Removing that verified
  commitment would undercount paid promises. The adapter prose was clarified
  and a regression added; runtime logic was not changed to follow the finding.
- R2 `5f6e7642-2873-45b5-a2cd-999859f22a4d` compared the accepted contract and
  exact executed evidence, returned **NO MATERIAL FINDINGS**, and explicitly
  withdrew the claimed policy violation. No unresolved material finding.
- R1 footprint: 4 calls; 66,811 cache-create, 130,291 cache-read, 18,388 output;
  maximum context 73,585. R2: 4 calls; 42,475 cache-create, 118,832 cache-read,
  5,494 output; maximum context 49,249. No usage warnings. Two completed
  Sonnet/high rounds, Read/Write only, limited to their named artifacts.
- All source changes and testing were performed by Codex. Claude wrote only
  the named response files. Both predecessor engines, live Contador script,
  website capacity ingress and container package are unchanged; independent
  runner build/email transport checks are not applicable to this pure slice.

## Remaining work registered at program closure

- `work:student-enrollment-transactional-store`: atomically persist both
  aggregates plus aliases, proofs, exceptions and outbox; prove retries and
  concurrent source/seat races in disposable PostgreSQL only.
- `work:student-enrollment-authenticated-source-admission`: create trustworthy
  proof records from authenticated native events/reads and named operator
  decisions, connect the transactional caller under an explicitly bounded pilot,
  and replace old funding writers without duplicate effects.
- Both remain candidates requiring separate authorization. The current slice
  neither claims these capabilities nor activates any runtime/provider path.

## Git and program closure

- Reviewed implementation `86141afa1ba941538b29c4f75fb56ed88a0eb6fd` is committed
  and pushed to `origin/codex/student-enrollment-ingress-adapters-20260906`.
  Commit-hook formatting changed no source; the isolated worktree was clean.
- Final staged continuity: 164 task rows / 160 changelog entries; schema
  sanitizer, generated capability matrix, targeted Prettier and diff checks pass.
- Company OS r230 applied through compare-and-swap from r229. All nine
  commitments are completed with evidence and the claim is null. The two exact
  follow-ups above remain unauthorized candidates; their dependency edges also
  prevent authority cutover from appearing independent of unfinished runtime
  integration. No strategy, authority boundary or concurrency limit was weakened.
- Primary governance mirror:
  `.program/evidence/NC-20260906-007-enrollment-ingress-adapters.md`.
- Final handoff:
  `/Users/xbohdpukc/dev/NanoClaw/handoffs/2026-09-06-2029-enrollment-ingress-adapters-complete.md`.
  Program validate/status and check-handoff pass at r230. Because the checker
  rejects terminal bindings, the checkpoint explicitly names completed adapter
  work and binds the unauthorized transactional-store candidate for orientation.
  No active/eligible work or downstream action authority is implied.
- Primary Git HEAD remains `51185a5d`; its dirty source was not edited, cleaned,
  reset or staged. Only governance/handoff files changed there. The new source
  is unwired; no database durability, live enrollment or provider outcome is claimed.
