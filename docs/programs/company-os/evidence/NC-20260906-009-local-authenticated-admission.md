# NC-20260906-009 local authenticated admission evidence

Company OS `work:student-enrollment-authenticated-source-admission`.
Decision `decision-enrollment-local-authenticated-admission-2026-09-06` records
the bounded keep-going instruction and explicit local/production split.

## Authority and worktree

- Program revalidated at r234 with no active claim. Existing mixed completion
  was split at r235: local foundation authorized; original consequential
  condition retained in `work:student-enrollment-production-admission-pilot`,
  unauthorized and a cutover prerequisite. Claim r236, nine commitments r237.
- Isolated `/Users/xbohdpukc/dev/NanoClaw-enrollment-admission-20260906` on
  `codex/enrollment-authenticated-admission-20260906` from reviewed `a382e215`.
  Registration committed/pushed before implementation; dirty primary source
  remains at `51185a5d` and is not used for source changes.

## Local implementation

See `docs/STUDENT-ENROLLMENT-AUTHENTICATED-ADMISSION.md`. Real native bindings
are not claimed. A registered host receipt protocol verifies raw HMAC bytes,
audience/transport, scope/purpose/role, exact source/offer/Party/seat/class facts,
receipt IDs and expiry. Keys are private copies and cannot be shared across
issuers/roles; operator privilege cannot be registered to provider transports.
Only private verified certificates reach the transactional admission service.

Local migration 147 stores immutable authenticated receipts and canonical writer
claims. Source receipt conflicts and writer/policy conflicts produce owned
exceptions without unauthorized enrollment effects. New control rows are read
back exactly in the same transaction as canonical state. Real legacy writers
remain unchanged; both fixture paths participate in the gate, and production
must prove both real paths do so before activation.

## Verification

- Pinned Node 22.23.2 dependencies and root typecheck; strict standalone worker
  typecheck. Focused 157/157 across admission/store/ingress/Bookkeeper/foundation/
  capacity checks. No real keys, credentials, source reads or customer fixtures.
- Full root: 3,723 pass / 32 skipped / two unchanged CNPC wrapper-literal and
  Trafft freshness failures (the established predecessor baseline).
- Unit negatives cover MAC tampering, unknown key/scope, actor/role/purpose/
  payload/audience/transport/time changes, key reuse, duplicate proof/receipt IDs,
  forged/cross-service/expired certificates, and configuration alias mutation.
- Disposable proof covers all three signed mock origins, exact retries and
  aliases, owner grants, partial sponsors, denied legacy overlap, a concurrent
  dual-writer race with exactly one effect, atomic rollback including control
  rows, lost COMMIT acknowledgement, expiry after connection but before writes,
  immutable receipt reuse, stale policy, deliberate control-row corruption,
  credential non-storage, populated rollback refusal and zero non-admin grants.
- The reused harness still creates its own fresh local database, strips PG
  routing/password sources, applies only local migrations, proves empty rollback/
  reapply, runs the isolated worker and verifies database removal. Store-mode
  regression remains green; its production target guard was not weakened.

## Independent review

- Fresh bounded Sonnet/high R1 `d219db82-9d85-43f1-8c2d-10999716b32b`, Read/Write
  only, four named implementation/proof artifacts and request, 100k compaction,
  through the installed `alex,info` runner. Verdict, usage, final verification,
  Git and program closure will be appended. No review success is assumed.

## Correction and final verification

- R1 claimed correction routing could duplicate a legacy enrollment. The exact
  disposable reproduction returned held correction_requires_resolution, zero
  new enrollments and unchanged legacy ownership/effect. Existing commercial,
  participant and assignment proof payloads already bound channel.
- A narrower funding-only routing authentication gap was real: the new negative
  test failed before correction. Every signed statement now includes channel
  and must match an immutable issuer channel allowlist. Provider issuers cannot
  register correction channels; grant channels require owner_admin. An explicit
  correction branch adds review/audit state only, with no ingress-engine call,
  writer claim or student effect. Operator review remains possible without
  taking over a legitimately legacy-owned source.
- R2 `424e6338-69b3-4d4c-95d7-766b0fc50bba` confirmed the narrowed finding and
  correction, returned **NO MATERIAL FINDINGS**. No unresolved material issue.
- R1 usage: 4 calls, 65,657 cache-create, 134,334 cache-read, 13,834 output,
  max context 72,431. R2: 4 calls, 53,280 cache-create, 126,962 cache-read,
  8,777 output, max context 60,054. No usage warnings; two Sonnet/high rounds.
  Claude wrote only its named review responses; Codex performed code/testing.
- Final focused: 159/159 (19 admission tests plus predecessor/store checks).
  Root and strict standalone worker typechecks pass. Final full suite with
  `npm test -- --maxWorkers=4`: 3,725 pass / 32 skipped / two known failures;
  347 test files pass, ten skipped, two fail. CNPC/Trafft failures reproduced
  on unchanged predecessor `a382e215` in this task (8 pass / 2 fail).
- A default-parallel run additionally timed out an unchanged capacity shadow
  disposable test at 5.18 seconds. It passed alone in 0.6 seconds and in the
  bounded full suite. This was handled as test contention, not silently added
  to the baseline or hidden by skipped assertions. No unrelated source or test
  timeout was changed.
- Both corrected review-only and legacy/new race paths are proven in actual
  disposable PostgreSQL; no real provider, credential, student or historical
  record was accessed, no production migration/activation occurred.

## Git and program closure

- Registration `8679b46e` and reviewed implementation
  `a46ecfe2c81e5aa624ee29472301e3d025a41d83` committed/pushed to
  `origin/codex/enrollment-authenticated-admission-20260906`; isolated checkout
  clean and commit-hook formatting left source unchanged.
- Company OS r238 closes all nine commitments and releases the claim. The
  owner-authorized split preserves the original live/native completion condition
  in `work:student-enrollment-production-admission-pilot`, still unauthorized
  and a prerequisite of authority cutover. No real credential, provider,
  migration, deployment, cutover, financial or communication authority expanded.
- Final continuity: 166 task rows / 162 changelog entries; schema sanitizer,
  capability mirror, format and diff checks pass. Program validate/status pass.
- Final checkpoint:
  `/Users/xbohdpukc/dev/NanoClaw/handoffs/2026-09-06-2345-local-authenticated-admission-complete.md`.
  Check-handoff passes at r238. The completed local item is explicit, while
  the checkpoint binds the existing unauthorized pilot for orientation because
  terminal work-item bindings are rejected by the checker. Nothing was reopened
  or claimed for production.
- Primary governance copy: `.program/evidence/NC-20260906-009-local-authenticated-admission.md`.
  Primary source stayed at `51185a5d` with its dirty changes preserved; only
  governance/handoff records changed there. Migration 147 and all cryptographic
  fixtures remain local/synthetic. No native provider or live outcome is claimed.
