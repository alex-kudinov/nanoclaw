# NC-20260907-001: supervision repair and product identity bridge

## Authority and lineage

Owner explicitly requested the exact repair and durable cross-system product
identity management on September 7. Company OS authorization:
`.program/decisions/decision-product-identity-repair-2026-09-07.json`.
Work: `work:student-product-identity-reconciliation`; first source is isolated
from `f5adc8cc`, following live `aa73538c84505212767628b81477e7d287e98af9`.
The dirty primary checkout and the undeployed admission/store branches remain
unchanged. No source migration or new enrollment writer activation occurred.

## Exact repair

- Product Map A157:C157 added exact full-payment supervision name → CSS →
  Coaching Supervision Mastery after blank-cell and target-header readback.
- Existing deployed host replayed case 63, with its original account/PI/event;
  lifecycle enqueue was suppressed and no customer communication was sent.
- Case version 1 / complete / attempt 2; six source/admission/accounting/roster/
  final receipts verified. CSS A6:C6 read back, Sales A16:F16 empty.
- Payment Log A469:K469 read back; original transaction date retained, recorded
  date advanced. PostgreSQL had exactly one payment totaling 399600 cents.
- No refund, additional charge, access grant or class/capacity assignment.

## Durable implementation

Existing entitlement offers/bundles remain canonical. New versioned bindings
reference their provider IDs and add account scope plus legacy roster targets.
Both supervision offers are migrated; display-name changes no longer control
their route. Unknown/mixed companion IDs, scope or projection conflicts and
incomplete source evidence hold registration without suppressing accounting.
The release builder requires the helper and both catalog files as tracked
runtime inputs.

The audit records 47 checkout products / 37 active, eight entitlement offers,
one active canonical route and 36 active migration gaps. `mcs-full` is missing
from the local checkout snapshot, not declared absent from the business.
Strict audit returns exit 2 for that full population and exit 0 for a synthetic
registered-only renamed-label population. Output contains only keys/statuses
and source hashes; source invitation codes are never exported.

## Verification and review

- 85 focused tests pass, including ten new identity cases.
- Typecheck and entitlement validator pass (42 components / 7 bundles / 8 offers).
- Full suite: 3630 passed / 32 skipped / two unrelated failures. Both CNPC
  wrapper and date-stale Trafft failures reproduced on unchanged f5adc8cc
  (8 passed / 2 failed in that focused baseline reproduction).
- Documentation continuity and capability checks pass after correcting the
  required active-work timestamp and detail entry.
- Sonnet/high R1 b40ad243-f615-4f5e-84dd-6e94d3b427c9: no material findings.
  Nine model calls; 77,942 cache-created input, 433,796 cache-read input,
  17,233 output, maximum context 77,944; no usage warnings. More context rereads
  than desired; the correction review is constrained to four reads.
- Codex independently corrected the Checkout PI/charge-read catch to set
  incomplete evidence instead of allowing a known line binding to bypass an
  unread conflicting metadata offer. Sonnet/high R2
  8735a1ad-8e68-4e1a-8819-e2456bb89fa8 verified the correction with no further
  material findings. Final focused 85 pass; full 3630 pass / 32 skip / two
  reproduced baseline failures. R2: four model calls, 37,313 cache-created input,
  114,758 cache-read input, 3,751 output, max context 44,087; no usage warnings.

## Deployed and verified

- Reviewed implementation 16171042; documentation whitespace closure produced
  exact release 138e43abb7a5845af9185c731e70189a35c9f95b, pushed and clean.
- Archive SHA-256 fab33497e21ba45ffb32dea979fc833af7e8fe048445443031688ab2bbc732da.
  Fresh local extraction and independent Mini checksum/runtime verification pass.
- Initial build required installing the separate agent-runner dependency tree;
  no source workaround. Standard release then passed its critical email tests,
  independent runner build and all 45 runner tests.
- Live Mini activation from aa73538c at 2026-09-07T14:22Z changed only the three
  declared service paths. Pending-send active-state drain was zero. Contador
  instructions were backed up and synchronized before activation; source and
  operational SHA-256 dc1629cd42fe70b7adf52e7de2e19aa4c7fa092ae41955ba3e5d13900b02b394.
- Rollback plist:
  `/Users/xbohdpukc/Library/LaunchAgents/com.nanoclaw.plist.rollback-aa73538c8450-2026-09-07T14-22-00-921Z`.
  Prompt backup: operational `.release-backups/NC-20260907-001/contador-CLAUDE.md`.
- Installed resolver loads binding revision 1, routes a renamed supervision
  label to CSS, and rejects a wrong-account alias. This is a read-only installed
  code check, not a manufactured purchase.
- A fresh host replay of complete case 63, with downstream tripwires, returned
  duplicateComplete=true and preserved state/version/attempt 1/2. No processor,
  lifecycle enqueue or capacity action was invoked. CSS, Product Map and empty
  Sales cells were independently read again after deployment.
- Health reports the exact verified release, matching code root and connected
  Gmail/Slack. A new natural post-release supervision purchase is not yet observed.

## Durable remainders

- `work:student-product-identity-coverage-rollout`: complete sellable offer and
  provider identity/payment-plan coverage, generated projections, and adoption
  of the strict coverage check in the owning publication workflow.
- `work:supervision-enrollment-capacity-reconciliation`: source-bound dated
  supervision class, participant roster and confirmed capacity; no guessed count.
- Existing `work:student-enrollment-production-admission-pilot` retains its
  separate migration, exact source/participant population, writer ownership,
  native authentication, rollback and live-projection gates.
