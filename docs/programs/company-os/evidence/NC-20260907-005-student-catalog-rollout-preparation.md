# NC-20260907-005 — first student catalog rollout preparation

Date: 2026-09-07

## Outcome

This source-only packet proposes the first deterministic catalog-publication
contract and selects exactly `supervision-inaugural` plus
`supervision-regular` for a future implementation. It is concrete enough for a
new authorized implementation task to begin coding from then-current NanoClaw
and Tandemweb production lineages. Nothing in this packet is applied.

## Why this population

The two routes are the current v1 bridge's only source-bound offer population.
They provide two distinct offers and native product/default-price identities,
one shared frozen bundle and CSS roster target, one active checkout case, and one
intentionally inactive checkout case. This makes them the smallest population
that can prove deterministic generation, exact existing-resolver parity,
consumer-specific coverage, and unsafe-lowering refusal without expanding paid
admission or provider fulfillment.

## Targeted technical findings

- Exact read-only Stripe reads reconfirmed both products in `stripe:alt`, their
  active native product state, and their exact native default-price identifiers.
  The documented toolbox has no exact price getter, so native amount, currency,
  recurrence, and duration remain unverified. Source declarations stay labeled
  as source declarations.
- Provider `whoami` confirmed the native account selected by the toolbox `alt`
  configuration. Processor source labels its separate `STRIPE_SECRET_KEY_ALT`
  slot `tandem`. This does not prove that the deployed consumer configuration
  selects the same native account. Fixture generation/parity may proceed, but
  activation remains held until an actual configured-consumer probe or equivalent
  non-secret configuration attestation plus provider readback closes the alias.
- Exact Heartbeat reads from the documented bizmgr context and one exact managed
  course manifest reconfirmed the PCC/MCC practice-test course and a same-label
  group candidate. Supported surfaces expose no group-course attachment. The
  candidate is not substituted for the absent historical catalog group and the
  gap stays a technical/source hold outside the first population.
- The initial Heartbeat call from the NanoClaw context returned `MISSING_CONFIG`;
  it is a context limitation, not a provider outage. The two prior Plutio generic
  exit-1 results were not repeated or reclassified.
- No people, customers, learners, members, progress, agreements, invoices,
  payments, or financial-record population was read. No provider write occurred.

## Contract and application

`docs/STUDENT-CATALOG-PUBLICATION-CONTRACT.md` assigns authority per fact,
requires typed namespace/object/relationship/evidence/source-version/effective-
period ownership, and names concrete future files in NanoClaw and Tandemweb. It
separates deterministic payload hashes from wall-clock receipts, preserves exact
current resolver results, rejects lossy lowering, and treats coverage as
consumer-specific only after activation and readback.

`docs/work-packets/NC-20260907-005-FIRST-APPLICATION.md` records exact positive
and negative acceptance. Inaugural stays active and regular stays inactive.
Native group/course existence never becomes Heartbeat attachment, access,
progress, class, or completion proof. Existing roster/accounting processing and
the unregistered legacy path remain unchanged.

Cross-repository publication is explicitly non-atomic. Every consumer continues
using its current facts until its current-lineage release pins and reads back the
exact reviewed artifact. Partial failure stops the sequence and leaves later
consumers on prior facts; rollback restores a prior known-good consumer artifact
without editing historical terms or replaying enrollments. The resolved
NC-20260907-001 production episode is not a canary.

## Decisions and independent holds

No new owner decision is required to begin the later first-slice implementation.
The account alias is a technical activation gate. D-01 locale promise/sale intent,
D-02 MCS promise equivalence after missing native evidence, and D-05 service
quantity/consumption policy remain real later business decisions. D-03 PCC
attachment and D-04 service-source discovery remain source work first. None
blocks supervision identity-consumer tooling.

## Authority and effects

The accepted NC-004 evidence commit is
`0e6fb6050a304689561234d8fdbb0282674ae5bd`; NC-005 claim commit `70e0bc6e` is
pushed on `codex/catalog-rollout-prep-20260907`. Astra owns the primary
`.program` evidence at
`.program/evidence/NC-20260907-005-rollout-preparation.md` and final program
reconciliation. This repository packet does not modify `.program`.

No canonical catalog, binding, checkout, source fixture, generator, runtime,
provider, roster, payment, capacity, schedule, message, migration, deployment,
publication enforcement, or consumer activation changed.

## Verification and review

Focused JSON, cross-link, privacy, resolver-parity, diff, and pinned-Node
continuity checks pass. Claude Sonnet/high R1 found one material missing evidence
citation for the supervision Heartbeat observations. The corrected technical
resolution now records exact accepted snapshot paths and recomputed digests,
observation time, native IDs, existence-only states, attachment false, and a
current-lineage refresh/acceptance gate. Narrow R2 returned
`NO MATERIAL FINDINGS`.

R1 used 4 model calls, 74,909 cache-create, 120,120 cache-read, 24,038 output,
and maximum context 74,911. R2 used 4 calls, 42,327 cache-create, 118,029
cache-read, 6,057 output, and maximum context 49,101.

Astra independently recomputed the two corrected NC-004 snapshot hashes, checked
the exact native group/course records and supervision fixture facts, confirmed
the consumer alias remains an activation hold, read R1/R2, and accepted this
packet for the final scoped source/continuity commit and push. Astra retains
primary `.program` and semantic-handoff closure.
