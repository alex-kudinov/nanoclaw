# NC-20260802-007/008 — Codex request for Claude C5 review R5

- Reviewer: Claude Code Opus 5, exact session
  `b361d68b-688c-4dd0-bba0-a43188673962`
- Review root: `/private/tmp/nanoclaw-sequence.oUOHVX/worktree`
- Base: `081d2a9b01bcf33dfbc4d9ba4815b0f6b21f8bf7`
- Prior verdict: R4 `APPROVE WITH FOLLOW-UPS`; NC-007 owns R4 N1/N3 and
  NC-008 owns R4 N2/N4/N5
- Class: C0 review only. Apart from the response report named below, do not
  change source or continuity files, stage, commit, build a release, invoke
  launchctl, post to Slack, mutate a database, or deploy.

## Objective

Adversarially review the complete uncommitted NC-007/008 delta above the stated
base. Determine whether it safely closes the deferred release-activation and
Sales-routing findings without weakening the one-root-per-work-item contract,
cross-channel isolation, retry idempotency, or atomic release activation.

Read `CLAUDE.md`, `docs/PROJECT-MAP.md`, `docs/ACTIVE-WORK.md`,
`docs/CHANGE-PROTOCOL.md`, the unreleased entries in
`docs/ENGINEERING-CHANGELOG.md`, `docs/RELEASE-INTEGRITY.md`,
`docs/SECURITY.md`, `groups/sales/CLAUDE.md`, the R4 request/report, and the
current code/tests. Treat the isolated review root above as the source tree;
do not review or modify the dirty shared checkout.

## Implemented delta

### NC-007

1. `release:activate` rejects an already-active target directory directly.
2. The executor uses macOS `/usr/bin/shlock` for an atomic PID-file claim and
   stale-PID takeover; cleanup removes only a lock still naming this process.
3. Dry-run now exercises the listener dependency probe.
4. Pruned rollback roots and healthy rollback reporting have direct diagnostics
   and assertions.
5. A Darwin-only integration test renders the real candidate plist XML and
   proves `plutil` lint/decode round-trip equality.

### NC-008

1. Scheduled marker equivalence expires after six hours; an older marker starts
   a new channel-root work cycle.
2. Same-lead Slack routing is serialized before anchor lookup and mutation.
3. Historical roots have negative tests for missing, cross-channel, threaded,
   non-work-item, and different-lead cases.
4. Queue-derived container context must be active. Same-channel Sales output
   may inherit its exact active work-unit thread; any explicit or inherited
   timestamp is stripped when the outbound channel differs from the source.
5. Resolver downgrade count/time and outgoing retry state are exposed as
   non-sensitive `/health` channel diagnostics.
6. Connected send failures get a bounded exponential retry trigger. If a long
   post established a root before a later chunk failed, only the unsent
   remainder is queued under that host-persisted root.

## Evidence already produced by Codex

- pinned Node 22.23.2 typecheck: pass
- combined focused suite: 7 files / 183 tests pass serially
- full suite: 144 files / 1,824 tests pass serially
- no release build, installed-service mutation, Slack post, or database write
  has occurred in this round
- the owner has now authorized deployment only after review, reconciliation,
  commit, exact release build, and pre-activation checks

## Required adversarial review

Please answer each question explicitly and cite exact file/line evidence:

1. Does `shlock` close the double-reclaimer race on supported macOS, and do
   acquisition, malformed/unreadable/live/stale PID handling, and owner-safe
   cleanup remain fail-closed? Check executable availability and filesystem
   assumptions, not only mocks.
2. Can dry-run, same-directory rejection, a pruned rollback root, or rollback
   reporting now produce a misleading successful rehearsal or remove the last
   known-good path?
3. Is the per-lead Promise-tail serialization correct for success and failure?
   Can resolver degradation, missing lead identity, or simultaneous scheduled
   posts still generate duplicate roots?
4. Is six hours a coherent work-cycle boundary given the timestamp actually
   stored and refreshed? Can it collapse a genuinely new cycle or split a
   revision, and do instructions/docs state that trade-off accurately?
5. Can the connected-send retry timer duplicate messages, spin, become
   permanently dormant, or reorder messages across reconnect/flush paths?
6. After a partial multi-chunk failure, does remainder-only retry preserve all
   unsent content exactly once beneath the established root, including a
   second failure during remainder delivery?
7. Can `source_container`, source-group lookup, inactive/adopted work, explicit
   timestamps, or non-Slack/Gmail routes cause a thread timestamp to cross a
   channel/group boundary or bind a message to the wrong active cycle?
8. Are the new `/health` fields non-sensitive and operationally sufficient to
   reveal resolver/retry degradation without making process-lifetime counters
   look durable?
9. Do source, tests, Sales instructions, project/security/release docs, active
   work, and changelog agree? Identify missing tests or documentation as either
   blockers or bounded follow-ups.
10. Is this delta ready to commit and then deploy through the reviewed atomic
    activator? Separate commit blockers, deploy blockers, and post-deploy live
    observations.

Re-run focused tests if useful. Write only the final report to
`/private/tmp/nanoclaw-sequence.oUOHVX/worktree/docs/reports/NC-20260802-007-008-CLAUDE-C5-REVIEW-R5.md`
with one verdict: `APPROVE`, `APPROVE WITH FOLLOW-UPS`, or
`CHANGES REQUIRED`. Separate blockers from follow-ups and identify any accepted
residual risk.
