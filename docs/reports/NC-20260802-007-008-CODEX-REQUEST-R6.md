# NC-20260802-007/008 — Codex request for Claude C5 review R6

- Reviewer: Claude Code Opus 5, exact session
  `b361d68b-688c-4dd0-bba0-a43188673962`
- Review root: `/private/tmp/nanoclaw-sequence.oUOHVX/worktree`
- Base: `081d2a9b01bcf33dfbc4d9ba4815b0f6b21f8bf7`
- Prior report:
  `docs/reports/NC-20260802-007-008-CLAUDE-C5-REVIEW-R5.md`
- Class: C0 blocker-closure review. Write only the response report named below;
  do not change source/continuity files, stage, commit, invoke launchctl, post to
  Slack, mutate a database, build a release, or deploy.

## R5 reconciliation to verify

1. The `shlock` mock now mirrors macOS by refusing every extant lock. Tests
   separately cover a live foreign PID, a dead PID, and a failed claim with no
   readable owner.
2. The executor reports those three states distinctly and never auto-unlinks an
   extant lock. Owner-checked cleanup remains unchanged.
3. Release/security/continuity docs now state the real platform behavior and
   require an operator to prove no activation command is running, remove only
   the exact reported `<plist>.activation.lock`, and repeat the full dry-run.
4. Dry-run checks `/usr/bin/shlock` executable availability as well as running
   the `lsof` probe. A missing lock tool has a direct error and a test.
5. After normalizing the installed root with `realpath`, the executor rejects a
   symlink alias of the active release, with a test.
6. NC-008's unregistered-source edge now strips explicit/inherited thread
   authority fail-closed, with a test. The health counter name says
   `SinceStart`, and docs call it process-lifetime. The Sales prompt says the
   six-hour window runs from root creation.
7. Resolver degradation still cannot establish the canonical email-based lead
   identity. It remains visibly unanchored and observable rather than minting a
   second identity; record that as a bounded follow-up unless it blocks this
   deployment.

## Independent evidence

- pinned Node 22.23.2 typecheck: pass
- focused delta: 7 files / 186 tests pass serially
- full regression: 144 files / 1,827 tests pass serially
- continuity check, schema sanitizer self-test, source formatting, and diff
  whitespace: pass
- no production mutation, Slack post, or database write in R5/R6

Inspect the current diff and answer:

1. Are R5 blockers B1 and B2 fully closed in code, tests, and authoritative
   docs without introducing a stale-lock race or misleading recovery claim?
2. Is F1 closed, and can dry-run still succeed when `shlock` is unavailable?
3. Do the F3/F4/F5/F6 corrections match runtime behavior and tests?
4. Is the remaining resolver-degradation behavior a deploy blocker, or a
   bounded fail-visible follow-up?
5. Is the exact delta ready to commit, build, and activate in production under
   the already granted owner authorization? Separate blockers, follow-ups, and
   post-deploy observations.

Write only
`/private/tmp/nanoclaw-sequence.oUOHVX/worktree/docs/reports/NC-20260802-007-008-CLAUDE-C5-REVIEW-R6.md`
with one verdict: `APPROVE`, `APPROVE WITH FOLLOW-UPS`, or
`CHANGES REQUIRED`. Cite exact current file/line evidence.
