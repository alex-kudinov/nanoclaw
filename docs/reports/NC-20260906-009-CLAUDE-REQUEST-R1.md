# Independent bounded authenticated-admission review

Read exactly these four artifacts once:
- `src/student-enrollment-admission.ts`
- `scripts/enrollment-admission-fixtures.mjs`
- `scripts/student-enrollment-admission-disposable-worker.ts`
- `docs/reports/NC-20260906-009-CRITICAL-DIFF.md`

Write material findings with precise evidence/reproduction, or NO MATERIAL
FINDINGS, to `docs/reports/NC-20260906-009-CLAUDE-RESPONSE-R1.md`, then stop.
Read/Write only; write only that response. No other reads, Bash/web/MCP, secrets,
credentials, real records or production/provider access. Stay under eight tool
calls. The bounded packet is approximately 46k characters.

Owner explicitly authorized ONLY a reversible local authenticated-admission
foundation using synthetic credentials and provider/operator mocks, plus review,
verification and Git handoff. The earlier mixed completion condition was split;
actual native provider bindings, real operator authentication, credential access,
production store promotion, migrations, writer participation and a new-event pilot
remain in a separate unauthorized item. Do not reopen that owner boundary.

This is an actual HMAC-SHA256 registered HOST-issuer receipt protocol, not a claim
of Stripe/Plutio native webhook compatibility. Mock issuers stand in for host
adapters which must verify native sources before issuing receipts in production.
No network handler or production mode exists. Real legacy writers are unchanged;
the guarantee is for participating callers, and the pilot must wire both paths.

Required local contract: exact raw-byte MAC and bounded schema; issuer scope,
transport, role and purpose binding; key/role isolation; fixed host catalog;
immutable authenticated receipt identity; private unforgeable certificates;
expiry recheck inside the transaction; canonical alias/writer identity; shared
legacy/new exclusion; one transaction for receipts/claims and canonical store
effects; exact control readback; no keys/signatures persisted; safe rollback and
uncertain-commit retry. Missing business facts remain owned holds, never inferred
students, grants or assignments. The operator is a registered principal, not a
role claimed in a request. Raw persistence primitives remain internal trusted
host code, not externally callable alternatives to authentication.

Accepted predecessor mechanics (do not re-read): the store enforces a generated
disposable database and Unix socket, uses SERIALIZABLE plus fixed ordered locks
over all 20 canonical tables, applies pure ingress, persists relational CAS
deltas, verifies readback, commits, and reports unknown commit acknowledgements.
The small callback seam retains that same transaction. Ingress preserves Party,
funding, offer, class and source-alias invariants and immutable intake decisions;
quarantines are owned. Source conflicts do not overwrite earlier truth.
Migration 146 supports evidence-key history and projection versions.

Evaluate the implementation, not just happy tests: MAC/capability bypass,
cross-issuer/role/source confusion, replay/receipt conflicts, transaction/lock
ordering, old-writer exclusion, rollback/readback, expiry and credential/data
containment. Worker tests exercise real disposable PostgreSQL and mocked legacy
effects. Unit tests additionally cover malformed signatures, payload/actor/role/
scope/audience/time changes, duplicate proofs, issuer key reuse, forged and
cross-service certificates. Focused 157/157, root/strict-worker typechecks pass;
full suite has only the two independently known predecessor failures. No
production or provider success is claimed. Sonnet/high, fresh bounded review.
