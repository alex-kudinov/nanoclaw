# NC-20260907-003 bounded routing and lifecycle strategy review

You are the independent reviewer. Report only material defects in the adopted
model-routing policy, owner-directed student lifecycle strategy/plan, decision
register/portfolio mapping, and first planned read-only reconciliation packet.
Do not perform broad repository archaeology, propose an alternate strategy, or
edit any file except the named response artifact.

## Objective and authority

The owner requested durable student lifecycle strategy and plan assembly after
adopting CoachGrader-style routing: Astra owns strategy/architecture/acceptance;
one explicit `gpt-5.6-sol` high-effort worker executes and verifies; Claude Code
Sonnet/high reviews independently. This is C1 policy/config/document work only.
The accepted Astra brief is source authority but is excluded from your read set
to keep this packet below 50,000 characters. Its binding requirements are stated
completely below.

## Accepted facts that must not be reopened

1. Extend existing entitlement/enrollment/capacity/Company OS authority. Providers
   keep native facts; no Sheet, Plutio, Heartbeat, or CoachGrader student master.
2. Bindings are typed, scoped, versioned relationships. IDs/groups/names/amounts/
   dates do not manufacture offer, purchase, participant, class, or progress.
3. One agreement/order has many obligations/receipts. Aliases, installments, and
   retries never duplicate orders, students, enrollments, or seats.
4. Migrate bounded populations through explicit legacy dispositions and a staged
   ratchet. The 47/37/one figures are a dated source snapshot only.
5. Preserve committed-seat simple sync and its accepted small race risk. Do not
   restore synchronous checkout reservations, short-lived holds, or a live
   NanoClaw checkout dependency.
6. Preserve live bridge `138e43ab`; integrate reviewed/unwired local enrollment
   tip `8b1ffbb9` (migrations 146/147 unapplied) from current lineage. Both descend
   from `f5adc8cc`; reconcile overlapping continuity files by meaning.
7. The recommended first pilot is a new fully settled self-purchased supervision
   episode with proved participant, dated class, and capacity. It is not currently
   authorized; do not replay Ma. Carmen. Accounting survives registration holds.
8. Later lifecycle requires documented progress/assessment/completion evidence
   and separately authorized service/certificate/marketing actions. Access is not
   progress; CoachGrader is assessment evidence; all existing action gates remain.
9. Revision 246 is canonical: one active claim and 17 candidates. Three unactivated
   priority-24 deltas would make 20: source reconciliation (`internal_reversible`,
   no authorization, depends strategy; prerequisites coverage/supervision/pilot),
   finance semantics (`internal_reversible`, no authorization, depends source +
   Bookkeeper contract), and projection reconciliation (`external_consequential`,
   unauthorized, depends admission pilot; gates full authority cutover). Preserve
   every existing pilot/capacity/control-plane/action/minion item.
10. One explicit Sol High execution writer; optional second child read-only unless
    isolated; no recursive Codex delegation or silent Astra fallback. One program
    claim. Resource-scoped claims remain proposed. Explicit model/effort is always
    required; defaults are defense in depth and fresh-task behavior is unverified.
    Astra owns `.program`; Sol reads its binding and registers repository work.
11. Canonical admission completes with canonical state plus a disabled/pending
    projection request, without roster/access or end-to-end claims. Separately
    authorized projection reconciliation follows admission and supplies readback.
    The core proves aliases of one fully settled receipt; distinct installment
    payments belong later and neither block nor broaden it. The local ingress has
    eight source-channel dispositions, including review-only correction.

## Allowed read paths

- This request.
- `docs/MODEL-ROUTING-POLICY.md`
- `docs/STUDENT-LIFECYCLE-STRATEGY.md`
- `docs/STUDENT-LIFECYCLE-DECISION-REGISTER.md`
- `docs/work-packets/NC-20260907-003-READONLY-CATALOG-RECONCILIATION.md`
- `.codex/config.toml`

The combined request/read set is below 50,000 characters. Do not read other
files, Git history, `.program`, credentials, settings, runtime state, or provider
data. The current Sol worktree contains uncommitted task files; make no source
edit.

AGENTS and CLAUDE bind the same role split; Codex will mechanically verify those
short links against the reviewed policy and the 200-line CLAUDE hook limit.

## Review questions

Report a material finding only if the supplied artifacts:

1. contradict or omit an accepted fact above;
2. conflate program/offer/agreement/receipt/bundle/component/access/class/progress,
   or allow a provider ID/name/amount/date to manufacture identity;
3. permit installments, aliases, retries, or partial failure to duplicate or lose
   funded promises, orders, participants, seats, accounting, or exceptions;
4. reintroduce superseded reservation/hold/live-checkout mechanics;
5. treat the undeployed enrollment branch or snapshot counts as live/current;
6. make a phase exit possible without the required negative, replay, readback,
   lineage, capacity, natural-event, or action-gate evidence;
7. make canonical admission depend on downstream projection, call canonical-
   only evidence end to end, or let multiple installment payments enter the
   fully settled pilot;
8. misstate the exact portfolio mapping or silently authorize a candidate;
9. make the first packet writable, privacy-expansive, dependent on unsupported
   Heartbeat surfaces/browser automation, or impossible to execute/review; or
10. allow model routing/configuration to expand program concurrency, permissions,
    release authority, put `.program` mutation on Sol, or silently fall back to
    Astra.

For each finding, give severity, exact file/section, conflicting accepted fact,
consequence, and the smallest concrete correction. Ignore prose preferences and
speculative backlog. If there is no material issue, write `NO MATERIAL FINDINGS`.

## Response

Write only
`docs/reports/NC-20260907-003-CLAUDE-RESPONSE-R1.md`. Include a compact review
scope receipt, findings ordered by consequence, and final verdict. Do not edit
the request or any reviewed source.
