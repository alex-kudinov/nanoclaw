# NC-20260907-005 Astra acceptance packet

Status: accepted for scoped source/continuity commit and push

## Proposed first application

Exactly `supervision-inaugural` and `supervision-regular` enter the first future
publication-tooling implementation. They preserve distinct offer identities, one
shared frozen bundle and CSS roster destination, inaugural active checkout, and
regular inactive checkout. The population proves schema/generation and exact
current v1 roster identity parity without changing current processor behavior or
expanding paid admission, access, fulfillment, capacity, or lifecycle coverage.

## Decisive contract

The proposed publication contract:

- leaves program, offer/bundle, checkout, provider, roster, agreement, and party
  facts with their current owners;
- requires typed namespace, object, relationship, evidence, source version/hash,
  effective-period, and owner fields;
- names concrete future NanoClaw and Tandemweb schema, fixture, generator, test,
  and consumer-output paths;
- separates deterministic payload hashes from wall-clock receipts;
- preserves exact resolver scope/identity/projection-conflict and legacy behavior;
- rejects lossy lowering of future many-to-many or interval-scoped bindings;
- uses per-consumer prepare/validate/review/stage/activate/readback states;
- keeps current consumer facts live until exact current-lineage activation and
  readback; and
- stops on partial publication and restores a prior known-good artifact without
  rewriting history or replaying enrollments.

## Technical resolution and holds

- Both supervision native products/default prices are exact in `stripe:alt`.
  Native amount, currency, recurrence, and duration remain unverified source
  limits.
- Processor source labels `STRIPE_SECRET_KEY_ALT` as `tandem`, while toolbox
  `alt` identifies the audited native account. The deployed consumer binding is
  unverified, so an actual configured-consumer identity probe or equivalent
  non-secret attestation plus readback blocks activation.
- Accepted NC-004 snapshot digests, observation time, and exact supervision group
  and course IDs prove separate Heartbeat object existence. Attachment, access,
  progress, class, completion, fulfillment, and global strict-publication status
  remain unverified.
- D-01 locale promise/sale intent, D-02 MCS promise equivalence after technical
  evidence, and D-05 service quantity/consumption policy remain later business
  decisions. D-03 PCC attachment and D-04 service-source discovery remain source
  work first. None blocks the first roster identity consumer tooling.

## Review and verification

Claude Sonnet/high R1 found one material missing evidence citation for the
supervision Heartbeat observations. The correction added exact accepted snapshot
paths, recomputed SHA-256 digests, observation time, object IDs, existence-only
statuses, `group_course_attachment_verified:false`, and a current-lineage
refresh/acceptance prerequisite. Narrow R2 returned `NO MATERIAL FINDINGS`.

R1 usage: 4 model calls; 74,909 cache-create, 120,120 cache-read, 24,038 output;
max context 74,911. R2 usage: 4 calls; 42,327 cache-create, 118,029 cache-read,
6,057 output; max context 49,101.

JSON, cross-link, privacy, no-`.program`, diff, source-digest/object, current
resolver 10/10, and pinned Node 22.23.2 continuity checks pass.

## Acceptance and effects

Astra independently recomputed both corrected NC-004 snapshot hashes, checked the
exact supervision native records and catalog fixture facts, confirmed the account
alias remains unverified for the deployed consumer, read R1/R2, and accepted the
first population and activation holds. Astra authorized one scoped final commit
and push of task-owned NC-005 documents and continuity only.

No application, canonical catalog/binding, source fixture, generator, production
code, provider, roster, payment, capacity, schedule, message, migration,
deployment, publication enforcement, checkout, or `.program` mutation is
authorized or performed. Astra retains primary program and handoff closure.
