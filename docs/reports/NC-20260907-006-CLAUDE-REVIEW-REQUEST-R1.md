# NC-20260907-006 bounded implementation review R1

Review only material correctness, safety, or activation defects in the final
two-route catalog publication implementation.

## Authority and exact revisions

- Accepted contract: `docs/STUDENT-CATALOG-PUBLICATION-CONTRACT.md` as corrected
  by current-source evidence in
  `docs/reports/NC-20260907-006-SOURCE-AUTHORITY-CORRECTION.md`.
- NanoClaw code revision: `efd7188b14714d259e205eadc99afc3cdc728a82` on
  `codex/catalog-publication-20260907`, based on live `1449812c1e71`.
- Tandemweb code revision: `7cc261e32a` on
  `codex/catalog-publication-current-20260907`, based on deployed/current-main
  `c61fabf84f45`.
- Current source semantics are accepted and must not be reopened: both routes
  active; inaugural allows only `2026-10-07`; regular excludes that date and
  serves later cohorts; both require an exact supervision cohort. Prices,
  installment roles, bundle, account IDs, and CSS roster target are fixed.
- Native amount/recurrence and Heartbeat attachment/access/progress/class/
  completion remain unverified. No payment, provider, roster, student, capacity,
  schedule, message, migration, or customer action is authorized.

## Bounded read projection

Read only this request and these exact ranges, under 50,000 total characters:

1. `/Users/xbohdpukc/dev/NanoClaw-catalog-publication-20260907/scripts/build-student-catalog-publication.mjs`
   lines 1-260, 296-630, and 670-920. These contain schema subset enforcement,
   pre-read allowlists/typed evidence checks, physical path/symlink checks,
   Nano-only release parity, source validation, and consumer projection.
2. `/Users/xbohdpukc/dev/NanoClaw-catalog-publication-20260907/scripts/reconcile-supervision-checkout-source.mjs`
   lines 1-315. This is the dry-run/default, exact-hash, backup-first operational
   mirror repair.
3. `/Users/xbohdpukc/dev/tandemweb-catalog-publication-current-20260907/tools/validate-student-catalog-publication.py`
   lines 1-220. This is the static Tandem checkout consumer.

Do not read other files, credentials, `.env`, auth/config stores, customer or
student data, provider data, transcripts, or broad repository history. Do not
use Bash, web, MCP, or network. Write only the response path below.

## Integration facts to assess

- Existing `loadProductBindings()` changed only from the editable v1 binding to
  the generated byte-equal compatibility artifact. Runtime compile/resolve code
  is unchanged.
- Nano release build runs `--check-nanoclaw` before build. The release inventory
  requires schema, manifest, generated binding, generator, alias/provider
  evidence, and mirror helper. Fresh archive verification runs the same
  Nano-only check from extracted bytes.
- Tandem `deploy.sh` uses `set -eo pipefail`, resolves one full
  `CANDIDATE_COMMIT`, archives that exact commit once, runs existing design
  governance and the new static validator in the same candidate directory,
  resets to that exact commit, then requires `HEAD == CANDIDATE_COMMIT` before
  continuing. It retains the current cache and ACC build logic.
- Generator file hashes: Nano compat
  `95611db848891d67c5b30c5b34be5d11250bf00adc2b8c80312bc1722fb27ae9`;
  Tandem file `9e6946410d25be5f8801d937ab40f0e347afa7ff834c26135cc94da93ce31db4`;
  embedded Tandem payload
  `70657a2cd41c624e0345a8ec2e0e72190a7e686eb9d6bb1f4d468f2e66d3ec78`;
  selected checkout
  `2235d83be9169ab668e0475e19bd79ce212d73ea2b8ff8b960bbc6cfe63a9723`.
- Focused checks: Nano publication/resolver/drift 50/50, mirror 4/4, fact sync
  8/8; Tandem 16/16, regional 38/38, installments 11/11; typecheck and continuity
  pass. Full Nano previously produced 3,656 pass / 32 skip / three failures.
  Exact production base independently reproduces the additional Capacity
  reserved expected 1 / actual 0 failure; CNPC and date-sensitive Trafft are the
  established predecessor failures.

## Required review

Check that manifest paths cannot read private files before validation; evidence
states cannot exceed sources; v1 lowering rejects lost account, interval,
many-to-many, target, or consumer meaning; deterministic outputs and price roles
are correct; regular eligibility cannot be confused with inaugural eligibility;
the release gates reject stale/tampered compatibility bytes; the static consumer
blocks selected drift without blocking unselected products; the deploy target is
pinned; and the mirror repair cannot overwrite concurrent or unrelated edits.

Report material findings only, in consequence order, with exact file/line
evidence and the smallest correction. If none, write `NO MATERIAL FINDINGS`.
Write the response only to
`/Users/xbohdpukc/dev/NanoClaw-catalog-publication-20260907/docs/reports/NC-20260907-006-CLAUDE-REVIEW-RESPONSE-R1.md`.
