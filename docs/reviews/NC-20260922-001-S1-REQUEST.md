# NC-20260922-001 minimum-sufficient necessity review

Return exactly one decision: `KEEP`, `DEFER`, `REMOVE`, or `OWNER`. Identify the
largest avoidable operational burden and the smallest feasible path. Review
only this packet and the listed authoritative files. Write the result to
`docs/reviews/NC-20260922-001-S1-RESPONSE.md`. Do not edit anything else.

## Objective

Recover one already-paid Tandem Commerce MCS installment through its original
idempotent Bookkeeper jobs, then stop the same class of permanent cross-system
contract failure from retrying indefinitely.

## Current observable result and evidence requirement

- Adyen API response and the authenticated signed webhook agree that one USD
  999 installment succeeded. Commerce materialized the paid order and completed
  Heartbeat invitation, lifecycle, billing activation, documents and receipt
  mail submission.
- `nanoclaw.bookkeeper.notify` and `nanoclaw.bookkeeper.fees` remain pending
  after repeated receiver rejection. They are downstream accounting/roster
  projections and cannot create another charge.
- The signed payment envelope contains the canonical MCS cohort shape:
  `program=mcs-practicum`, `module=0`, `full_program`, twelve sessions and an
  `mcs-practicum-<24 hex>` key. The deployed receiver admits only
  `acc|pcc|actc`, modules 1-4 and four sessions, so it rejects before any
  Bookkeeper write.
- The exact PSP is absent from the 513-row Payment Log. The fee-only recorder
  requires one exact Payment Log PSP row, so it cannot reconcile before the
  payment receipt succeeds.
- Evidence required for recovery: original job IDs complete; exactly one
  Payment Log PSP row with USD 999 gross and Adyen provenance; exact MCS roster
  destination/cohort readback; one matching PostgreSQL projection; exact
  retained provider plus estimated Peri fee/net readback; no new payment,
  refund, email, enrollment or access mutation.

## Constraints and present hazards

- Payment and signed webhook truth must not be reopened or replayed.
- The Product Map and Student Roster remain destination authority. Cohort writes
  stay fill-only and must use exact signed evidence.
- Reuse the existing signed receiver, Commerce jobs table/runner, recorder and
  deployment workflow. No new schema, route, queue, worker, scheduler or
  service.
- Preserve the active `NC-20260921-001` incomplete-checkout work; this recovery
  branch changes only the Bookkeeper receiver/tests/docs before the first live
  recovery.
- A permissive generic cohort acceptance could route fabricated metadata; an
  unbounded retry continues noisy deterministic failures; producer-first
  deployment could terminalize work under an incompatible consumer.

## Operational-obligation delta and counterfactuals

1. Extend the existing receiver's bounded cohort union with the already-known
   MCS Practicum shape. If removed, the paid receipt remains permanently
   unprojected. If delayed, no safe exact recovery exists. Keep this inside the
   current signed receiver rather than creating another adapter.
2. After the transaction is recovered, classify receiver outcomes so permanent
   authenticated contract rejection stops in operator-visible attention while
   transient transport/runtime failure keeps bounded backoff. If removed, the
   current infinite retry/noise remains. This can reuse existing job state and
   does not require a new queue or scheduler.
3. Make the existing fee job eligible only after the corresponding payment
   projection completes. If removed, one root failure continues to produce a
   second dependent failure. Keep this as an eligibility check over existing
   jobs, not a new orchestration service.
4. Add a production-shape cross-contract regression containing MCS full-program
   cohort plus fee dependency/error classification. If removed, per-repository
   tests can again pass while the deployed producer and consumer disagree.

## Next falsifiable integration proof and bounded work

Stage 1 changes only `src/commerce-bookkeeper.ts`, its focused test, and required
continuity/release records. Deploy the consumer first, verify health, then allow
only the existing two jobs to replay and read back all four Bookkeeper sinks.

Stage 2 may change only the existing Commerce Bookkeeper transport/job
eligibility/retry code and focused tests, plus NanoClaw's safe response contract
if necessary. It must distinguish permanent versus transient outcomes, bound
retries, gate fee work on payment completion, and pass a producer-envelope to
consumer-parser contract fixture. No new topology or customer/provider action.

## Authority and allowed sources

- `src/commerce-bookkeeper.ts`
- `src/commerce-bookkeeper.test.ts`
- `tools/contador/process-commerce-payment.cjs`
- `facts/programs.yaml`
- `facts/generated/student-product-bindings-v2.scoped.json`
- `docs/PROJECT-MAP.md`
- `docs/ACTIVE-WORK.md`
- `docs/ENGINEERING-CHANGELOG.md`

Do not inspect secrets, `.env`, runtime databases, customer data, auth stores or
unrelated repository files. Do not authorize or perform deployment, payment,
refund, email, enrollment, roster or accounting writes.
