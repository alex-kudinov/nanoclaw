# NC-20260907-005 execution assignment

Status: in progress; preparation only

## Assignment

`/root/catalog_rollout_packet` is the single `gpt-5.6-sol` high-effort execution
writer. Astra owns strategy, `.program`, and acceptance. Claude Code Sonnet/high
provides independent bounded review. The isolated worktree is
`/Users/xbohdpukc/dev/NanoClaw-catalog-rollout-prep-20260907`, branch
`codex/catalog-rollout-prep-20260907`, from exact clean base
`0e6fb6050a304689561234d8fdbb0282674ae5bd`. Claim commit `70e0bc6e` is pushed.

## Outcome

Prepare a reviewed and executable first catalog-publication packet from the
accepted NC-004 audit. The packet resolves supported technical gaps, defines one
source/version/projection publication contract, and specifies the inaugural and
regular Coaching Supervision Mastery routes as the first unapplied population.
It does not apply that population or alter current behavior.

## Required artifacts

- proposed publication contract:
  `docs/STUDENT-CATALOG-PUBLICATION-CONTRACT.md`;
- exact first application:
  `docs/work-packets/NC-20260907-005-FIRST-APPLICATION.md`;
- compact technical resolution:
  `docs/reports/NC-20260907-005-TECHNICAL-GAP-RESOLUTION.json`;
- real business decisions only:
  `docs/reports/NC-20260907-005-BUSINESS-DECISIONS.md`;
- aggregate evidence, validation receipts, independent review, and continuity
  updates.

## Accepted facts and bounds

- NC-004 is accepted at `0e6fb605`; its 56 source records, 37 active checkout
  records, eight offer keys, and provider/source limitations are evidence, not a
  new catalog or allowlist.
- The first population is exactly `supervision-inaugural` and
  `supervision-regular`. Preserve inaugural active and regular inactive checkout
  states. A native Stripe product remaining active does not activate checkout.
- The current v1 bridge uses the configured key `tandem`; NC-004 observed the
  native provider namespace `stripe:alt`. Publication must validate their exact
  alias-to-native-account relationship instead of renaming either identity.
- Every supplied offer/product/price signal for a matched v1 route must resolve
  to that one route. Incomplete or unknown co-signals, wrong scope, and a
  differing live Product Map target hold. Unregistered populations retain the
  current legacy path.
- Native group and course existence are separate observations. Neither proves
  group-course attachment, participant access, progress, class assignment, or
  completion.
- Publication across repositories is staged. A prepared or generated artifact
  is not deployed coverage; each consumer continues using its current facts
  until its compatible pinned artifact is activated and read back.
- No production code, canonical source, provider, user/access, roster, payment,
  capacity, schedule, message, migration, deployment, publication-enforcement,
  primary-checkout, or `.program` write is in scope. No learner, customer, or
  financial-record population is read.

## Verification and review

Run focused JSON, cross-link, coverage, privacy, unsafe-lowering, and diff checks,
then `./scripts/with-pinned-node.sh npm run docs:continuity-check`. Send one fresh
bounded packet of no more than eight artifacts and 50,000 characters to Claude
Sonnet/high without Bash, network, secret, or broad write access. Fix verified
material findings; use a second round only for a load-bearing correction. Astra
accepts the completed draft before the final task source/continuity commit and
push.
