# Courses Minion — Learned

## Pipeline Lessons (2026-04-07)

### Model: Use Opus, Not Sonnet
- Sonnet hallucinates attribution (e.g., claimed "ICF recognizes five group typologies" when Kalina never said that)
- Sonnet invents terminology the instructor didn't use ("typologies" instead of "types of groups")
- Sonnet adds counts not stated ("five types" when nobody counted)
- **Both process_one.py AND enrich_session.py now default to Opus**

### Recap Grounding Rules (in STUDENT_RECAP_PROMPT)
- Every claim must trace to something explicitly said in the transcript
- Use the instructor's own language — do not upgrade or embellish
- Do NOT attribute ideas to organizations (ICF, etc.) unless the speaker explicitly did
- Do NOT invent terminology or add counts not stated
- If source/authority wasn't stated, present as "what was covered in class"

### Email Rendering: Format Tolerance
- Recap markdown format varies between models (numbered vs bulleted, `**Title** —` vs `**Title.**` vs `**Title**:`)
- All regexes handle colon `:`, em dash `—`, en dash `–`, hyphen `-`, and period `.` separators:
  - Key Concepts: `- **Title** — Desc`, `**Title.** Desc`, `**Title**: Desc` (with/without bullet)
  - Instructor Highlights: same flexible pattern including colon separator
  - Reflection Questions: `1. question` and `- question`
- No manual recap format fixing needed — distribute_session.py handles all variants (fixed 2026-04-14)

### Free Course Validation (added 2026-04-14)
- distribute_session.py validates free-course.json entries against course.json before rendering
- Invalid lesson_id entries are auto-rejected (logged to stderr)
- Titles are overwritten with canonical versions from course.json — prevents hallucinated titles
- Static fallback `valid-lesson-ids.json` ships with enricher scripts (available in container)
- If ALL entries are rejected, free course section renders empty — report this, do NOT fabricate replacements
- Root cause of prior hallucinations: enrich_session.py had wrong path to course.json (fixed 2026-04-14)

### Email Validation (10 checks, gates all sends)
1. No unreplaced `{{PLACEHOLDER}}` tags
2. All 4 required sections have content (Key Concepts, Instructor Highlights, Go Deeper, Reflection Questions)
3. No raw markdown in rendered HTML (`**`, `*`, `- `, `#`)
4. Key Concepts count matches recap
5. Instructor Highlights count matches recap
6. At least 2 reflection questions
7. Article URLs have UTM params
8. Matched `<strong>`/`<em>` tags
9. Session title present and >10 chars
10. Free course units rendered if section header present

### Instructor Handling
- Instructor auto-excluded from student recipient list
- Instructor gets BCC copy on distribution
- Instructor LinkedIn resolved from `data/instructors/{slug}.json`

### .env Sourcing Bug
- NanoClaw `.env` has unquoted `ASSISTANT_NAME=Mr Gru` which breaks `source .env`
- `distribute_session.py` runs send-email.sh with `cwd="/tmp"` to avoid sourcing NanoClaw's `.env`

### NEVER Rewrite recap.md
- The courses minion must NOT rewrite or "improve" recap.md
- It was generated by the enrichment pipeline and is the source of truth
- Agent rewrote it after first render, causing stale email HTML with empty sections

### Enrichment Directory Naming: Date-Only Causes Overwrites (2026-04-08)
- Pipeline uses date only as directory key (e.g., `2026-04-07`)
- Multiple sessions on the same day silently overwrite each other
- Fix needed in transcript worker: use date + session slug or timestamp (e.g., `2026-04-07-level2-module2` or `2026-04-07T15:03`)
- Until fixed: only one session per day can be enriched; second run destroys the first

### heartbeat.py Instructor Skip Fix (2026-04-07)
- Summary files use "Kalina (Instructor)" not "Kalina Terzieva" — normalized raw name is "kalina", not "kalina terzieva"
- SKIP_NAMES has "kalina terzieva" — raw-name check fails
- Fix: after matching attendee to a Heartbeat user, also check the user's normalized name against skip_names
- Kalina is now auto-excluded via the instructor resolution path without needing `--exclude`

## 2026-04-08 — Level 1 Module 4: Metaphors in Coaching

- Instructor: Kalina Terzieva, PCC
- 3 recipients: Frances Powell, Mark Bob, Misha E.
- Distributed successfully
- Heartbeat instructor auto-exclusion fix applied (user-name skip check)

## 2026-04-06 — Module 1 Session 1: Coaching Foundations: Asking Transformational Questions

- Instructor: Karen Bruns, PCC
- 9 recipients matched from Heartbeat
- Email rendered successfully; preview NOT sent — EMAIL_USER/EMAIL_PASS not configured in container
- Distribution blocked pending email credential setup

### Issues Encountered

1. *TOOLBOX_LIB path wrong* — Script computes `TOOLBOX_LIB = TOOLBOX_ROOT / "lib"`, but container has toolbox libs at `/workspace/extra/toolbox-lib/`. Fix: symlink `/workspace/extra/lib -> /workspace/extra/toolbox-lib` and set `TOOLBOX_ROOT=/workspace/extra`.

2. *INSTRUCTORS_DIR not set* — Defaults to `~/dev/tandemweb/data/instructors` which doesn't exist in container. Fix: always set `INSTRUCTORS_DIR=/workspace/extra/instructors`.

3. *EMAIL_TOOL not set* — Defaults to path inside non-existent toolbox. Fix: set `EMAIL_TOOL=/workspace/extra/email/send-email.sh`.

4. *Kendra Nitta* — Not in Heartbeat, will not receive emails until registered.

### Required env vars for distribute_session.py

```
TOOLBOX_ROOT=/workspace/extra
EMAIL_TOOL=/workspace/extra/email/send-email.sh
INSTRUCTORS_DIR=/workspace/extra/instructors
```

## 2026-04-14 — Level 2 Module 2: Team Coaching Agreements & the STORMS Model

- Instructor: Kalina Terzieva, PCC
- Recipients: Robin Nordmeyer, Zara Hughes, Thomas Goldstein, Christine Fredley, Scena Webb (5 total)
- free-course.json extraction failed (1 failed) — manually authored 3 units based on session content
- Corrective resend required for Thomas Goldstein + Christine Fredley due to concurrent agent running distribution with old heartbeat.py before nickname fix was applied

### Issues Encountered

1. *lib/ directory not persisted between sessions* — heartbeat.py must be recreated on every cold start. Added to MEMORY.md.

2. *Single-token substring matching bug* — old heartbeat.py matched "Tom" → "Chen Tom" (substring match) instead of "Thomas Goldstein". Fixed by using whole-word matching for multi-token queries and _NICKNAMES dict for single-token (tom→thomas). Now scores thomas goldstein 0.70 vs chen tom 0.65 (last-name fallback), so Goldstein wins.

3. *free-course hallucination fixed 2026-04-14* — enrich_session.py had wrong path to course.json (missing `community/icf/` prefix), so the LLM got no course structure and fabricated lesson IDs. Path fixed. distribute_session.py now validates all entries against course.json (or static `valid-lesson-ids.json` fallback in container). Invalid entries are auto-rejected. Do NOT manually author free-course.json — if extraction fails, report the failure.

4. *Sena listed first-name-only* — Scena Webb is in Heartbeat (scena.webb@outlook.com). Use full name "Scena Webb" in meeting summaries going forward.

5. *Concurrent agent runs* — two Gru sessions ran simultaneously and caused conflicting distributions. Meeting summaries should include full names to prevent ambiguous first-name matching under any version of heartbeat.py.

## Session: 2026-05-05 Level 2 Module 3 Class 1 (distributed 2026-05-12)

- Session title: Coaching Frames for Team Coaching (Kalina Terzieva, PCC)
- 3 recipients: Zara Hughes, Robin Nordmeyer, Thomas Goldstein
- Enrichment dir: 2026-05-05-1458
- lib/ directory was missing on container restart — recreated heartbeat.py from memory (standard procedure)
- No matching issues; all three students resolved cleanly

## Session: 2026-05-11 Module 2 — Coaching Presence and the Power of Silence (distributed 2026-05-12)

- Instructor: Karen Bruns, PCC
- Enrichment dir: 2026-05-11-1448
- 10 recipients: Kayla Bigerton, Pharri Kenwood, Rachel Bari Keane, Aradhana Goel, Misha E., Chong Chen, Jackie Epler, Edward Utz, Richard Dean (Rick), Karen Fenstermacher
- lib/ directory missing on container restart — recreated heartbeat.py from scratch (standard procedure)

### Issues Encountered

1. *Karen Fenstermacher incorrectly included* — "Karen (Tandem Instructor)" in attendee list normalizes to "karen" (single token, parens stripped). SKIP_NAMES had "karen bruns" but not "karen". Single-token match scored Karen Fenstermacher 0.70 (first-name match). She received the email in error.
   - Fix: heartbeat.py now checks raw_name for "instructor"/"admin"/"facilitator" marker BEFORE normalizing — those attendees are skipped entirely
   - Rule: meeting summaries should always list instructor as "Karen Bruns (Tandem Instructor)" not just "Karen (Tandem Instructor)" to give the instructor name for SKIP_NAMES exclusion — but the raw-name marker check is now the primary guard

2. *Ed Utz and Rick (Richard Dean) matched correctly* — they were listed as unmatched in the preview (which ran with a different/earlier heartbeat.py). On final distribution, Ed Utz matched by full name and Rick matched via _NICKNAMES rick→richard.

3. *Preview vs. final count mismatch* — Preview showed 7; final distribution sent to 10. Discrepancy due to heartbeat.py being rebuilt for each cold start without a persistent canonical copy. The preview was generated in a prior container session with a different heartbeat.py version.

## Session: 2026-06-19-1758 — ICF MCS Enrollment Consultation with Debbie Harbec (skipped)

- Instructor: Cherie Silas
- Type: 1-on-1 enrollment consultation (prospective student)
- No distribution — not a group class, no student cohort
- recap.md flagged correctly: pipeline noted the session format doesn't map to a class recap
- Precedent: same disposition as 2026-05-19 (Ed Millermaier consultation)

## Session: 2026-06-30-1400 — ACTC Team Coaching Program Inquiry Call (skipped)

- Instructor: Cherie Silas
- Attendee: Emilie Mcgrath (prospective student, holds PCC)
- Type: 1-on-1 program inquiry call (ACTC program structure, scheduling, pricing)
- No distribution — not a group class, single prospective student
- Note: meta.json listed session_type "class" but meeting summary had meeting-type "1on1" and purpose "prospective student inquiry" — content disposition takes precedence over meta.json session_type field

## Session: 2026-07-01-1144 — Contract Exit, MCC Review, and Supervision Course Prep (skipped)

- Participants: internal staff (contract/staffing/logistics discussion)
- Type: internal 1-on-1 operational meeting
- No distribution — no students, no teaching, no coaching concepts
- Pipeline flagged correctly in recap.md; 2 extractions also failed (consistent with non-class content)
- meta.json listed session_type "class" — recap.md content disposition takes precedence

## Session: 2026-07-10-1136 — ICF Session Observation Form Redesign and Coach Grader (skipped)

- Participants: Alex + colleague (internal operational meeting)
- Type: internal 1-on-1 — form redesign on tools.tandemcoach.co, Coach Grader workflow, product strategy
- No distribution — no students, no instructional content
- Pipeline flagged correctly in recap.md; 1 extraction failed (free-course, consistent with non-class content)
- meta.json listed session_type "class" — recap.md content disposition takes precedence

## Session: 2026-07-10-1202 — Polish ICF Foundation Course Licensing Discussion with Pavel (skipped)

- Participants: Cherie, Alex, Pavel (vendor/partner)
- Type: licensing and translation partnership discussion — not a coaching class
- No distribution — no students, no instructional content
- Pipeline flagged correctly in recap.md; 2 of 5 extractions failed (consistent with non-class content)
- meta.json listed session_type "class" — recap.md content disposition takes precedence
