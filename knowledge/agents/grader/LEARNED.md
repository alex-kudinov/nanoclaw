# Grader minion — learned lessons

(Append durable grading lessons the instructor confirms — e.g. a recurring misread, a
calibration refinement — so they carry across runs. Keep it short.)

- **Confirm the assignment BEFORE grading (2026-07-12, Alex).** Grader used to grade a
  wrong-module submission (e.g. Module 4 content against Module 5 requirements), deliver
  a verdict, and only THEN mention a possible discrepancy. Now a hard pre-grade gate
  (CLAUDE.md Step 4 + Critical Rule 7): detect the mismatch first, flag it, STOP, and
  grade only after the user confirms which assignment to grade.
